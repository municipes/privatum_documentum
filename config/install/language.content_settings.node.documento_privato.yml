langcode: it
status: true
dependencies:
  config:
    - node.type.documento_privato
id: node.documento_privato
target_entity_type_id: node
target_bundle: documento_privato
default_langcode: site_default
language_alterable: false

